let names = ["Alice", "Bob", "Charlie", "David", "Emma", "Frank"];
// replacing elements from index 2 with the two names
// console.log(names.splice(2,2,'saiprasad','vamsi'));

// deleting last two elements
// console.log(names.splice(-2));

// reversing an array
// console.log(names.reverse());

// sorting an array based on dictionary order.
// console.log(names.sort())

// sorting an array reverse to the dictionary order.
// console.log(names.sort((a,b)=>a-b));

